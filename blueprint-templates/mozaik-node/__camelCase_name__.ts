import { types } from '@mozaikjs/core'

interface {{camelCase name}}Props {}

export const {{camelCase name}}Model = types.model<{{camelCase name}}Props>('{{camelCase name}}', {

})

export const {{camelCase name}}Instance = {{camelCase name}}Model.create({

})
